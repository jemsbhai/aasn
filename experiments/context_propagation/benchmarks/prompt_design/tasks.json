[
  {
    "id": "prompt_001_code_gen",
    "type": "function_test",
    "prompt": "Write a Python function called `merge_sorted(a, b)` that merges two sorted lists into a single sorted list without using the built-in sort.",
    "test_cases": [
      {"call": "merge_sorted([1, 3, 5], [2, 4, 6])", "expected": "[1, 2, 3, 4, 5, 6]"},
      {"call": "merge_sorted([], [1, 2])", "expected": "[1, 2]"},
      {"call": "merge_sorted([1], [])", "expected": "[1]"},
      {"call": "merge_sorted([1, 1], [1, 1])", "expected": "[1, 1, 1, 1]"}
    ]
  },
  {
    "id": "prompt_002_analysis",
    "type": "contains",
    "prompt": "Explain the difference between a stack and a queue. Give one real-world example of each.",
    "expected": ["LIFO", "FIFO"]
  },
  {
    "id": "prompt_003_reasoning",
    "type": "contains",
    "prompt": "You have 8 balls, one of which is heavier than the rest. Using a balance scale, what is the minimum number of weighings needed to find the heavier ball? Explain your strategy.",
    "expected": ["2"]
  },
  {
    "id": "prompt_004_code_debug",
    "type": "function_test",
    "prompt": "The following function has a bug. Fix it and return the corrected function.\n\ndef binary_search(arr, target):\n    low, high = 0, len(arr)\n    while low < high:\n        mid = (low + high) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            low = mid\n        else:\n            high = mid\n    return -1\n\nThe function should return the index of target in arr, or -1 if not found.",
    "test_cases": [
      {"call": "binary_search([1, 2, 3, 4, 5], 3)", "expected": "2"},
      {"call": "binary_search([1, 2, 3, 4, 5], 1)", "expected": "0"},
      {"call": "binary_search([1, 2, 3, 4, 5], 6)", "expected": "-1"},
      {"call": "binary_search([], 1)", "expected": "-1"}
    ]
  },
  {
    "id": "prompt_005_summarize",
    "type": "contains",
    "prompt": "In exactly two sentences, explain what a hash table is and why it is useful.",
    "expected": ["key", "value"]
  }
]
